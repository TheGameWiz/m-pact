# Memory Contract

The startup contract has moved to `references/startup-contract.md`.

The full operating protocol has moved to `references/full-memory-contract.md`.

Load `startup-contract.md` during refresh. Load `full-memory-contract.md` only for protocol repair, complex memory operations, or suspected drift.
